# PROJECT_STATE.md

Project purpose:
- Repository name suggests a price calculator (`Preis-Rechner`), but no source files or README currently exist. Actual product scope is not yet documented.

Current state:
- Repository was empty at setup time.
- Git has been initialized for structured work with `main` as the stable branch and `dev` as the working branch.
- Root control files and Copilot instructions now exist.

Key modules or areas:
- No application modules exist yet.
- Current known areas are repository governance and session handoff files in the root plus `.github/copilot-instructions.md`.

Completed work:
- Initialized Git repository structure.
- Established `main`/`dev` branch workflow.
- Added AI workflow control files and a minimal `.gitignore`.

Known issues:
- No `README.md`.
- No application code, entry point, or architecture implementation.
- No build, test, or lint commands discovered.

Current focus:
- Define the actual project scope and technical stack.
- Add initial project files or import the existing project content if this directory was meant to contain one.

Risks and uncertainties:
- Repository intent is only weakly inferable from the folder name.
- No files exist yet to confirm language, framework, or deployment model.
- Branch policy is configured, but there is no baseline application commit history yet.
